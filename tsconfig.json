{
  "compilerOptions": {
    "target": "ES2022",
    "lib": [
      "ES2022",
      "DOM"
    ],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": [
      "vitest/globals",
      "node"
    ]
  },
  "include": [
    "src"
  ],
  "_comment": "tools/ holds vendored copies of the monorepo's packaging scripts, run through tsx. They are not plugin source and were written against the monorepo's compiler settings, so they are not typechecked here under stricter ones."
}
